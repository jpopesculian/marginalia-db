{
  "name": "marginalia-db",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "harvest": "node scripts/harvest-iiif.mjs",
    "verify-links": "node scripts/verify-links.mjs",
    "fonts": "node scripts/install-fonts.mjs",
    "initials": "node scripts/extract-initials.mjs",
    "foliation-probe": "node scripts/foliation-probe.mjs",
    "prepare-data": "node scripts/build-data.mjs",
    "dev": "npm run prepare-data && vite",
    "build": "npm run prepare-data && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.8.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.2",
    "opentype.js": "^2.0.0",
    "vite": "^7.1.4"
  }
}
